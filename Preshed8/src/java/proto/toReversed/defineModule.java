package matrix.gnu.bin.proto.toReversed;

import java.util.AbstractList;

import javax.swing.text.html.parser.Element;

public class defineModule {
 
    private static final int Integer = 258512*100;

    public static boolean m(Integer i, Element element, @SuppressWarnings("rawtypes") AbstractList argList) throws Exception {
        if (Integer == i.intValue()) {
            element.getContent();
            packages(argList);
        }
        return false;

    }

    @SuppressWarnings("unchecked")
    private static boolean packages(@SuppressWarnings("rawtypes") AbstractList argList) {
        return argList.add(Integer);
    }


}
