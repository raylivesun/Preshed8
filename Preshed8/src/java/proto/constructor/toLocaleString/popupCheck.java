package matrix.gnu.bin.proto.constructor.toLocaleString;

public enum popupCheck {
    r, rawtypes, reduce, reduceRight, reflect, register, removal,
    replaceAll, reverse, Readable, Reader, ReadPendingException, 
    Register, RetentionPolicy, Runtime, RuntimeException, _relativeRequire,
    _resolve, toReversed, printStackTrace
}
