package matrix.gnu.bin.proto.constructor.freeze;

import java.lang.annotation.RetentionPolicy;
import java.util.Spliterators.AbstractLongSpliterator;

import matrix.gnu.bin.proto.constructor.entries.M;
import matrix.gnu.bin.proto.constructor.getOwnPropertyDescriptor.buffer;

public record L() {
    @SuppressWarnings("unlikely-arg-type")
    public static void Log(RetentionPolicy xoffset, M bM, AbstractLongSpliterator U) throws Exception {
            if (xoffset != null) {
                xoffset.describeConstable();
                xoffset.compareTo(xoffset);
                xoffset.equals(U);
                xoffset.hashCode();
                xoffset.name();
                xoffset.notify();
                xoffset.notifyAll();;
                xoffset.toString();
                
            } else if (bM != null) {
                bM.equals(U);
                bM.getClass().equals(U);
                bM.hashCode();
                buffer.STR.getClass().equals(U);
                buffer.StackTraceElement.toString();
                buffer.String.getClass().getName();
                buffer.StringBuffer.getClass();
                buffer.valueOf(null);
                buffer.values();
            } else if (U != null) {
                U.hashCode();
                U.characteristics();
                U.equals(U);
                U.estimateSize();
                U.getClass().equals(U);
                U.notify();
                U.notifyAll();
                U.toString();
            }
       }
}
