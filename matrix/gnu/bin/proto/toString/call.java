package matrix.gnu.bin.proto.toString;

import javax.swing.text.html.HTML;

public enum call {
    Object, ObjectInputFilter, ObjectOutput, OutputStreamWriter,
    Override, obj, objects, org, output, onmessage;

    public static boolean then(MatchException exception) throws Exception {
        if (exception == null) {
            return false;
        }
        return false;
     }

    /**
     * @param object2 
     * @deprecated Use {@link onmessage#onmessage(HTML,Object)} instead
     */
    public static void onmessage(final HTML message, java.lang.Object object2) throws Exception {
    	call.onmessage(message, null);
    }   
}
