package matrix.gnu.bin.proto.every;

public enum resolver {
    getDetail, getCancelable, getClassLoader, getTarget,
    generated, securityContext, security, loadForeignModule,
    length, let, link, letters, local, complete, concat, concatCheck,
    children, concatCheckOutput, concurrent, constructor, CloneNotSupportedException,
    ConcurrentMap, Config, Counter, pConfig, isComplete
}
