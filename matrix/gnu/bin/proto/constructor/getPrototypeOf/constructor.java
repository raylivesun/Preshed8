package matrix.gnu.bin.proto.constructor.getPrototypeOf;

public enum constructor {
   Callable, Check, ClassFileFormatVersion, 
Constructor, CloneNotSupportedException, ConcurrentMap,
Config, Counter, cannot, canSetParameter
}
