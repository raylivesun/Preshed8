package matrix.gnu.bin.proto.constructor.entries;

import matrix.gnu.bin.proto.flatMap._loadModule;

public record M() {
       @SuppressWarnings("unlikely-arg-type")
    public static void MatchException(Math eMath) throws Exception {
            if (eMath != null) {
                eMath = valueOf(eMath, false);
                Math.min(0, 0);
                Math.max(0, 0);
                _loadModule.U = eMath.getClass().equals(eMath);
            } else if (eMath == null) {
                valueOf(eMath, false);
            }  
       }

    private static Math valueOf(Math eMath, boolean b) {
        // Auto-generated method stub
        throw new UnsupportedOperationException("Unimplemented method 'valueOf'");
    }
}
