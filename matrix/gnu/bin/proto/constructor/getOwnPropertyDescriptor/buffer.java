package matrix.gnu.bin.proto.constructor.getOwnPropertyDescriptor;

public enum buffer {
    s, StackTraceElement, STR, String, StringBuffer, StringTemplate,
    SuppressWarnings, System, setTarget, scopeName, scriptLoader, security,
    securityContext, setStackTrace, shift, skip, slice, some, sort, splice,
    sql, stack, stackTrace, standard, str, str2, stream, strict, string, stub,
    swing, symbols, toSorted, toSpliced
}
