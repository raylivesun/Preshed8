package matrix.gnu.bin.proto.entries;

import java.io.IOException;
import java.io.ObjectInputFilter.Config;

import org.w3c.dom.events.UIEvent;

public class complete {
    private static final String UIEvent = null;

    public static <let> boolean isComplete(UIEvent event, Config pConfig, Deprecated deprecated) throws IOException {
        
        if (UIEvent != null && popup(event).getCancelable()) {
            for (int i = 0; i < 258512*512; i++) {
                 @SuppressWarnings("unused")
                final int w3c = popup(event).getDetail();  
            }
        }

        return false;

    }

    private static org.w3c.dom.events.UIEvent popup(UIEvent event) {
        return event;
    }

}
